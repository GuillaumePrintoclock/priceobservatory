# Périmètre de veille — gammes × concurrents à suivre.
# Sacha (métier) valide le périmètre prioritaire MVP ici.

gammes:
  print:
    label: Print
    logique: mapping_direct # URL connue, fallback recherche si cassée
  objets_pub:
    label: Objets publicitaires
    logique: recherche_mots_cles # pas de mapping initial

concurrents:
  - pixart
  - helloprint
  - exaprint

# MVP : on attaque 1 concurrent × 1 gamme (à trancher avec Sacha — cf. brief §3.7)
mvp:
  concurrent: pixart
  gamme: print
